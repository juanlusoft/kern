{
  "name": "kern",
  "private": true,
  "type": "module",
  "scripts": {
    "check:boundaries": "node scripts/check-boundaries.mjs",
    "test": "npm run check:boundaries && node --import tsx --test scripts/check-boundaries.test.mjs scripts/export-pacoprint-holded-corpus.test.mjs scripts/label-pacoprint-holded-corpus.test.mjs && node --import tsx packages/compliance-tests/src/run-tests.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "pg": "^8.22.0"
  }
}
