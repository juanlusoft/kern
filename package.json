{
  "name": "kern",
  "private": true,
  "type": "module",
  "scripts": {
    "check:boundaries": "node scripts/check-boundaries.mjs",
    "check:client-boundaries": "node scripts/check-client-boundaries.mjs",
    "preflight:numa-postgres": "node --import tsx scripts/preflight-numa-postgres.ts",
    "test": "npm run check:boundaries && npm run check:client-boundaries && node --import tsx --test scripts/check-boundaries.test.mjs scripts/export-pacoprint-holded-corpus.test.mjs scripts/label-pacoprint-holded-corpus.test.mjs scripts/preflight-numa-postgres.test.ts && node --import tsx packages/compliance-tests/src/run-tests.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "pg": "^8.22.0"
  }
}
