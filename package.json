{
  "name": "kern",
  "private": true,
  "type": "module",
  "scripts": {
    "check:boundaries": "node scripts/check-boundaries.mjs",
    "test": "npm run check:boundaries && node --test scripts/check-boundaries.test.mjs && node --import tsx packages/compliance-tests/src/run-tests.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}
